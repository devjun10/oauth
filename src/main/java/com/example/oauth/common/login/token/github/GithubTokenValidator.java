package com.example.oauth.common.login.token.github;

import com.example.oauth.common.login.token.TokenValidator;

public class GithubTokenValidator implements TokenValidator {
    @Override
    public String validate(String jwtToken) {
        return null;
    }
}
